import { useLoaderData } from "react-router-dom";


const Booking = () => {

    const movie = useLoaderData();

    return (
        <div>
            
        </div>
    );
};

export default Booking;